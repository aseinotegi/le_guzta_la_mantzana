# Production environment variables for Netlify
# VITE_API_URL debe estar indefinido o vacío para usar URLs relativas
# Las URLs relativas como /api/subscribe funcionan con los redirects de Netlify
# Netlify redirige /api/* → /.netlify/functions/* según netlify.toml
