# Production environment variables
# No definimos VITE_API_URL para usar URLs relativas en producción
# Las URLs relativas como /api/subscribe funcionarán automáticamente
# Netlify redirigirá /api/* a /.netlify/functions/* según netlify.toml
